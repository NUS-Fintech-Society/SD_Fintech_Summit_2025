import React from "react";

const RegisterButton: React.FC = () => {
  return (
    <button
      style={{ width: 200, height: 60 }}
      className="bg-blue-500 text-white font-semibold w-358 py-2 px-4 rounded-full hover:bg-blue-600"
    >
      REGISTER
    </button>
  );
};

export default RegisterButton;
